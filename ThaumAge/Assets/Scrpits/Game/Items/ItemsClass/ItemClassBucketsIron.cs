using UnityEditor;
using UnityEngine;

public class ItemClassBucketsIron : ItemBaseBuckets
{


    public override void GetSomething(ItemsBean itemData, Vector3Int targetPosition, Vector3Int closePosition)
    {
        base.GetSomething(itemData, targetPosition, closePosition);
    }
}